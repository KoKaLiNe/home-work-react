import React from "react";
import { AppRoute } from "../../const";

const CreateBtn = () => {
    return (
        <button name="control" className="btn-add">Создать</button>
    )
};

export default CreateBtn;